api_lb_listen_address: "{{ kubernetes_api_endpoint.address }}"
api_lb_listen_port: "{{ kubernetes_api_endpoint.port }}"

api_lb_backends: "{{ kubernetes_api_backends }}"

api_lb_ingress_workers: "{{ kubernetes_api_workers }}"

api_lb_prometheus_port: 8404

api_lb_wazuh_indexer_port: 9200
