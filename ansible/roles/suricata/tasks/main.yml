---
- name: Install Suricata
  ansible.builtin.dnf:
    name: suricata
    state: present

- name: Keep Suricata disabled until the correct interface/path is selected
  ansible.builtin.systemd:
    name: suricata
    enabled: true
    state: stopped
