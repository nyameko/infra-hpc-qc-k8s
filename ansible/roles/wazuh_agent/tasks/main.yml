---
# Wazuh installation/repository details will be selected per environment.
- name: Record Wazuh manager target
  ansible.builtin.copy:
    dest: /etc/wazuh-agent-target
    mode: '0644'
    content: "{{ wazuh_manager_address }}\n"
