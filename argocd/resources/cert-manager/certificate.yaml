apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: traefik-test-tls
  namespace: default

spec:
  secretName: traefik-test-tls

  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer

  dnsNames:
    - traefik-test.quantum.nyameko.com
