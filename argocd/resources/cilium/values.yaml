# Project-specific Cilium configuration.
# Keep Helm chart defaults out of Git unless they are deliberate project choices.

cluster:
  name: kubernetes

operator:
  replicas: 1

routingMode: tunnel
tunnelProtocol: vxlan

# Enable Cilium agent metrics for Prometheus/Grafana observability.
prometheus:
  enabled: true
